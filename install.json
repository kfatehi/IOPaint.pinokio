{
    "run": [{
      "method": "shell.run",
      "params": {
        "venv": "env",
        "params": { "message": "pip install iopaint" }
      }
    }, {
      "method": "input",
      "params": { "title": "Install Success", "description": "Go back to the dashboard and launch the app!" }
    },{
      "method": "browser.open",
      "params": {
          "uri": "/?selected=LamaCleaner"
      }
    }]
  }
  
